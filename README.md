How to install requirements.txt (BACKEND) run command => pip -r requirements.txt <br>
How to install node_modules (FRONTEND) run command => npm install <br>
Run command (BACKEND) python manage.py makemigrations <br>
Run command (BACKEND) python manage.py migrate <br>
<h2 style="color:red"> Must Have PostgreSQL <h2 style="color:red">
